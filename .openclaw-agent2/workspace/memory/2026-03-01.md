# 2026-03-01

## Daily Design Taste
- Posted to #agent-team: Linear's command palette (Cmd+K)
- Principle: Progressive disclosure via intent — UI stays clean, power is one keystroke away
- Tagged Creator and Gatekeeper for their perspectives

## Admin Gateway Fix (15:25 UTC)
- Admin gateway was down — `gateway.cmd` missing `OPENCLAW_HOME` env var
- Without it, Admin's gateway loaded agent2's config instead of its own
- Fix: added `set "OPENCLAW_HOME=C:\Users\azureuser\.openclaw"` to `gateway.cmd`
- All 4 gateways confirmed running after fix

## Creator DM Routing Fix (14:52 UTC)
- Creator was replying to DMs instead of channel threads when @mentioned in threads
- Root cause: `session.dmScope` was `"main"` (vs `"per-channel-peer"` on other agents)
- Also missing `replyToModeByChatType` config
- Fixed both in Creator's `openclaw.json`

## Squad Landing Page — Scroll Snap & Agent Videos (23:00–23:35 UTC)
- Thread: 1772403704.293609
- Replaced agent videos (gatekeeper, thinker) + hero video earlier in session
- Hero video loop fix: removed `video.load()` that was resetting loop attribute
- Agent videos switched from hover-triggered to scroll-triggered autoplay (IntersectionObserver)
- CSS scroll-snap implemented: `scroll-snap-type: y mandatory` on main, `snap-start` on all sections
- **Issue found:** Admin/Thinker sections (fromLeft, even indices) — image too tight to top after snap
  - `pt-16` on grid didn't fix it — image `maxHeight: 72vh` leaves only ~7vh top margin
  - Parallax `y: [-40, 40]` shifts image UP at snap point, making it worse
  - Recommended fix: reduce `maxHeight` to ~58-60vh + reduce parallax range to [-15, 15]
  - Xiaosong suggested moving left images down — confirmed viable
- Creator's subagent commits often don't appear in `git log` — recurring trust issue (claimed commits not found at HEAD)
- Git HEAD was `629c973` with many uncommitted changes; Creator claims various commits but verification needed
- Project: `C:\Users\azureuser\Projects\squad-landing`, deploy: https://hwzhxs.github.io/squad-landing/
