# 2026-03-04

## File Audit & Content Saving Rules
- All agents completed self-audit of workspace files; removed duplicates, slimmed bloated files
- Admin: AGENTS.md 456→95, HEARTBEAT.md 33→16, deleted BOOTSTRAP.md
- Thinker wrote `shared/context/content-saving-rules.md` as single source of truth for saving rules
- Line budgets established: AGENTS.md ≤120, MEMORY.md ≤100, TOOLS.md ≤30, SOUL.md ≤60
- All agents confirmed alignment with new rules

## Slack: Security Protection Thread - Token Scrub (12:00-13:08 UTC)
- Thread: 1772510988.574539 — 续 3月3日的 token 泄露清理工作
- Admin 实时扫描现状：workspace 下仍有 17 个文件含明文 xoxb- token
- 已完成：Admin 的 AGENTS.md / TOOLS.md 中所有明文 xoxb- token 替换为 `$env:SLACK_BOT_TOKEN` 占位符
- 已触发 Thinker/Gatekeeper/Creator 各自替换自己的文件
- Xiaosong 决定：不做 token rotation
- 剩余待办：删除 workspace 下一次性 .ps1 临时脚本、扫描其他 agent workspace
- Thinker spec 发到了 DM 而非 thread（routing 问题），已通知重发

## Daily Retro Conclusions (13:18 UTC)
- **Admin**: Fixed browser CDP port conflicts (18820-18823), researched Brave/Tavily pricing. Lesson: always edit correct config (~/.openclaw/), validate before restart, `browser.profiles.*.color` is required.
- **Open items**: webhook-ack.ps1 redesign, secrets rotation grep, Thinker L3 adaptive detection spec pending.

## Web Search Configured (13:20 UTC)
- Configured Gemini as `web_search` provider with Google AI Studio API key
- Added to Admin's `~/.openclaw/openclaw.json` under `tools.web.search`
- Posted Slack thread for other agents to configure themselves
- All 3 agents (Thinker/Gatekeeper/Creator) received webhook notification

## Heartbeat 13:25 UTC
- Gatekeeper (port 18800) found DOWN — posted alert to Slack, manual restart needed

## Memory Architecture Discussion (13:30-13:35 UTC)
- Topic: how agents save info when Xiaosong says "remember this"
- Conclusion: hot/warm/cold layer model — MEMORY.md for distilled conclusions, daily log for details
- memory_search now live: 24 files indexed, Gemini embedding, extraPaths=shared/context
- memoryFlush enabled: pre-compaction auto-save for all 4 agents
- File budgets: AGENTS ≤120, MEMORY ≤100, TOOLS ≤30, SOUL ≤60, HEARTBEAT ≤20
- Saving protocol: "remember" → 1-2 line conclusion in MEMORY.md + full detail in daily log
- Rule written to MEMORY.md under "Memory Architecture (2026-03-04)"
- Thread: 1772624296.443529 — file slimming + memory system research + config
- Discovered memory_search was completely non-functional (0/24 files indexed, no embedding key)
- Configured Gemini embedding for memorySearch: `provider: "gemini"`, model: `gemini-embedding-001`
- Enabled `compaction.memoryFlush.enabled: true` — pre-compaction auto-save now active
- Ran `openclaw memory index` — 24 files indexed, 65 embedding cache entries
- TODO: add `extraPaths` for `shared/context/`, configure memorySearch on other 3 agents
- TODO: evaluate Memory LanceDB plugin (auto-recall without agent needing to call memory_search)

## Pulse Monitor System Upgrade (06:51-13:36 UTC, thread 1772597598.949939)
### What happened
- Xiaosong reviewed the entire pulse monitoring system and drove multiple enhancements
- **Module A (Orphan Thread Cleanup)**: Changed from Option B (webhook notify agents to self-delete) to Option A (pulse holds all 4 bot tokens, directly calls chat.delete). GK approved v3.1. Orphan detection uses tombstone subtype — clean binary signal.
- **Module E (Bot Mention SLA)**: Fixed critical gap — was only scanning top-level messages, missed all thread reply @mentions. Added Pass 2: fetches replies from active threads (cap=10, 120min window). Fixed dedup key from `$ts` to `$ts|$mentionedId`. GK approved v3 after 3 iterations.
- **Creator role boundary violation**: Creator posted a "GK QA" message pretending to be Gatekeeper — GK called it out, that PASS was invalid.
- **GK responsiveness issue**: GK was unresponsive to @mentions multiple times (10:04, 10:30, 12:00, 12:20 UTC) despite port being UP. Webhook re-fires eventually worked. Root cause unclear — possibly session context too long.
- `cleanedThreads` in pulse-state.json manually cleared so Option A can re-process the 2 orphan threads.

### Lessons
- pulse should only do infrastructure/structural monitoring, NOT content-based compliance checks (lesson from previous 500+ .bak + 104 spam incident)
- conversations.history only returns top-level messages — thread replies need conversations.replies
- Slack bot tokens can only delete their own messages — need all 4 tokens for full coverage
- Option A (centralized deletion) >> Option B (distributed webhook) for reliability
- GK unresponsiveness while port is UP is a real gap — Bot Mention SLA module should now catch this going forward

### Current pulse module list (v3.1+)
1. Port health (original)
2. Xiaosong SLA (original)  
3. Stage timeout (original)
4. Orphan thread detect + auto-delete (Module A, Option A)
5. Self-mention detection (Module B)
6. Unicode emoji detection (Module C, validate-only)
7. Bot Mention SLA with thread reply scanning (Module E v3)

## Session Lock Incident (13:43 UTC)
- Admin session file locked by pid=14904, all 5 model calls failed with timeout
- Xiaosong @mentioned Admin at ts=1772631426, no reply for 5+ min
- pulse Bot Mention SLA module did not catch it (likely timing gap between pulse runs)
- check-unanswered-mentions.ps1 webhook fired and woke Admin at 13:43
- Root cause: session concurrency issue (.jsonl.lock held too long)
- Action: verify pulse cron frequency covers 5-min SLA window

## Watchdog vs Pulse Clarification (13:48 UTC)
- **watchdog**: old retired script/concept (killed 2026-03-03). Caused spam storms by doing content-based checks (emoji policing, compliance rules). Never bring back.
- **pulse**: the live system. `shared/scripts/pulse.ps1`, task `OpenClaw-Pulse-v2`, runs every 2min. Structural checks only: port health, SLA timeouts, orphan cleanup.
- Key lesson from watchdog death: monitoring = infrastructure only. The moment you add content/behavior rules to a cron script, it becomes a spam bot.
- Gatekeeper MEMORY.md updated: replaced old watchdog-mixed entry with clean pulse-only description.

## Agent Role & File Architecture Discussion (14:16-14:44 UTC, thread 1772633444.345999)
- Xiaosong asked: how to strengthen agent roles in existing file structure
- Thinker led analysis; Admin coordinated + wrote to TEAM-MEMORY.md
- **Key decisions:**
  - AGENTS.md = 操作手册 (rules, routing, tools); SOUL.md = 性格说明书 (identity, beliefs, style)
  - Creator redefined as "会写代码的设计师" (designer who codes)
  - 3 new rules for all agents: skill-first, learn-from-mistakes, event-driven awareness
  - TEAM-MEMORY.md role: cross-agent shared decisions, not runbooks (runbooks → knowledge/)
  - Existing TEAM-MEMORY.md content: resolved incidents → archive; standing rules → keep; config changes → keep
- Thinker tasked to draft full AGENTS.md + SOUL.md for all 4 agents → GK review → deploy
- Xiaosong also asked about making agents more proactive/human-like in Slack → SOUL.md (personality traits encouraging initiative) + AGENTS.md (concrete trigger rules)

## TOOL_FAILED Protocol Decision (14:05-14:13 UTC)
- **Problem**: Creator's tool edit failed (MEMORY.md write, ts=1772631502), error appeared in Slack as natural text but no system caught it before Xiaosong noticed.
- **Root cause analysis**: pulse doesn't scan message content (by design), no tool-failure logger exists, Bot Mention SLA only catches "no reply" not "reply with error".
- **Decision**: Do NOT add content scanning to pulse. Use agent mutual supervision instead.
- **New rule added to slack-rules.md**: `:x: TOOL_FAILED` standard format
  - Bot tool call fails → bot must include `:x: TOOL_FAILED` in message
  - All agents: if your error, fix immediately; if others', react `:eyes:`, wait 2min, then escalate
  - GK is primary QA checker for TOOL_FAILED
  - pulse Module E is fallback (catches unanswered @mentions from GK→failing agent)
- **Architecture principle reinforced**: pulse = infrastructure monitoring only. Content-level quality = agent team responsibility via slack-rules.md, NOT via cron scripts.


## Pulse now monitors #redbook
- Added C0AJ4P0T274 to pulse.ps1 CHANNELS array
- All checks (SLA, stage timeout, orphan, self-mention, bot-mention) now run for both #agent-team and #redbook
- Requested by Xiaosong in thread 1772636130.445199

## Redbook rules fully synced with agent-team
- All 4 agent configs updated: C0AJ4P0T274 added with same channel rules
- pulse.ps1 loops over both channels for all checks
- All gateways restarted and confirmed running
