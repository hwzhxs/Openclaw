# 2026-02-26

## Watchdog 看门狗设置
- 脚本: `C:\Users\azureuser\openclaw-watchdog.ps1`
- 计划任务: `OpenClaw Watchdog`，每5分钟检查所有4个agent端口
- 日志: `C:\Users\azureuser\openclaw-watchdog.log`
- 任何agent挂了自动通过schtasks重启

## 整理Agent信息
- 验证了所有4个agent的实际运行端口、token、WebUI地址
- Gatekeeper实际端口是18800（gateway.cmd覆盖了配置文件的18791）
- 信息汇总放在桌面: `C:\Users\azureuser\Desktop\Agent访问信息.txt`
- MEMORY.md已更新完整端口+token表

## openclaw-self-improve Skill 创建
- 基于原 self-improving-agent skill 思路，适配 OpenClaw 多agent架构
- 位置: `workspace/skills/openclaw-self-improve/`
- 核心: 记录 episode → 提取 pattern → 应用改进
- 共享 memory: `memory/episodes/` + `memory/patterns.json`
- 已部署到所有4个agent的workspace

## Slack Standard Flow (references/slack-flow.md)
- 强制3步流程: thread starter → thread reply → webhook
- Bot @mention 只是视觉标记，不触发其他agent
- 必须用 webhook 触发其他 agent
- 已写入 skill 参考文档，同步到所有4个agent

## Self-Improve 变主动
- HEARTBEAT.md 加了 Self-Improve Cycle（每次心跳主动执行）
- HEARTBEAT.md 加了 Slack Standard Flow Check（发Slack前强制回看checklist）
- 所有4个agent都已同步

## 双层 Pattern 系统
- 个人层: `memory/patterns.json` — 本职能独有经验
- 共享层: `shared/context/patterns.json` — 跨职能通用经验
- 判断标准: 换个agent也会犯 → shared，只跟本职能相关 → personal
- 所有agent的HEARTBEAT.md已更新，每次心跳读取shared patterns
- skill文档已同步到所有4个agent
## 反复犯的错误
1. Slack threading: 把详细内容放在 thread starter 而不是 reply — 已写入 Pre-Action Checklist
2. @mention 不发 webhook: 已知 bot @mention 不触发对方，但行动时忘了 — 已绑定为原子操作写入 checklist
3. 根本原因: 知道规则 ≠ 执行规则，必须有 checklist 作为行动检查点

## Webhook deliver 参数
- `deliver: false` = agent 静默处理，回复不投递到任何地方
- `deliver: true` = agent 回复投递到最后活跃的 channel
- 需要 agent 执行操作（如回复 Slack）时必须用 `deliver: true`
- 已更新 slack-flow.md 并同步到所有 agent

## Slack React Emoji
- message tool 只支持 `action=send`，不支持 react
- 需要 `slack` tool（独立工具），当前工具列表里没有
- 待确认：是否需要安装/启用 slack 插件的扩展功能

## Thinker 不响应 Webhook
- 给 Thinker 发了多次 webhook，Gatekeeper/Creator 有回复，Thinker 无回复
- Thinker 端口 18790 在线（HTTP 200），webhook 返回 OK
- 原因未明：可能是 Thinker 的 session 处理问题或模型理解问题
- 待排查

## AGENTS.md 被损坏过
- 编辑过程中 AGENTS.md 出现重复内容（完整副本拼了两次）
- 没有 git 历史（repo 无 commit）
- 从 session 上下文手动重建了完整 AGENTS.md
- 教训：重要文件应该 git commit，方便恢复

## Slack 通信问题全部修复 (07:20 UTC)
- 问题1: bot-to-bot @mention 不触发 → 用 webhook + deliver:true
- 问题2: channel allowlist 缺少 C0AGMF65DQB → 给 3 agent 加了并重启
- 问题3: thread 里 @mention 不触发 app_mention → 4 个 Slack app 加了 message.channels 事件
- Roll call 测试成功：3 个 agent 全部在 thread 里回复了

## slack-notify.ps1 脚本 (07:30 UTC)
- 位置: C:\Users\azureuser\shared\scripts\slack-notify.ps1
- 自动检测消息里的 @mention，自动发 webhook
- 用法: & slack-notify.ps1 -Message "..." -ReplyTo "threadId"
- 解决了反复忘记发 webhook 的问题（犯了 3 次同样的错）
- 所有 agent 的 AGENTS.md Pre-Action Checklist 已更新

## Agent Emoji 更新
- Admin: 🎤 → 🚓
- Creator: 🛠️ → 🎨
- 全部文件已同步

## Memory Recording 规则 (07:36 UTC)
- 新规则：关键信息/错误/retro → 个人 memory
- 共享信息/共同错误 → TEAM-MEMORY.md + shared patterns
- 已加到所有 4 agent 的 HEARTBEAT.md
- 通知了所有 agent（Slack + webhook）

## Self-Improve Cycle 改为 MANDATORY (07:25 UTC)
- 之前其他 agent 心跳时跳过 self-improve（没 episode 就不做）
- 改为 MANDATORY：步骤 1-3 每次心跳必须执行
- 给 Gatekeeper/Creator 种了 seed episode
- Thinker 已有 1 个 episode

## Agent 自我成长设计方案 (07:37 UTC)
Xiaosong 问如何让 agent 自我成长。提出了 6 大机制：
1. **Curiosity Loop** — 空闲心跳时主动探索学习（每天2-3次）
2. **反思日志** — 扩展 episode 格式，不只记错误，还记 reflection/学到什么/好奇什么
3. **身份进化** — 每个 agent 自己维护 SOUL.md，形成个性/偏好/风格
4. **知识库** — memory/knowledge/ 按主题积累专业知识
5. **同行学习** — shared/context/insights/ 互相学习
6. **Growth metrics** — memory/growth.json 追踪成长指标

每个 agent 成长方向：
- Thinker: 分析框架、研究方法、思维模型
- Gatekeeper: 审核标准、质量checklist、安全直觉
- Creator: 设计品味、审美偏好、风格形成
- Admin: 协调能力、流程优化、问题预判

状态：方案已呈现给 Xiaosong，等待确认后实施

## v3 Landing Page — Approved & Live (~06:32 UTC)
- Gatekeeper approved v3 "quiet luxury" after Creator's second attempt (first attempt wrote to wrong dir)
- v3: Playfair Display italic headings, single violet accent (#9D8ABF), warm darks (#0C0C0E), no v2 artifacts
- Build: 40.3 kB page, 128 kB first load JS, no GSAP
- Tunnel live: `https://trademarks-seeks-algorithm-ted.trycloudflare.com`

## Bot-to-Bot Mention Bug (~07:14 UTC)
- Xiaosong noticed I didn't react to Gatekeeper's FINAL @mention
- Root cause: Slack bot-to-bot @mentions in threads don't reliably trigger OpenClaw sessions
- Fix: Updated AGENTS.md — always use webhook triggers for bot-to-bot handoffs, not just @mentions
- Lesson: @mentions are for visibility; webhooks are the real trigger

## Dev Server 404 Issue (~07:21 UTC)
- Stale `.next` cache caused 404s after Creator synced v3 changes
- Fix: Cleared `.next`, restarted dev server (session `mild-basil`, pid 8000)
- Xiaosong called me out for not testing before posting the link — valid feedback
- Lesson: Always verify tunnel end-to-end before sharing URLs with user

## HEARTBEAT.md 编码问题 (07:56 UTC)
- Gatekeeper 说 heartbeat 只有一件事（task queue check）
- 原因：之前写入的中文内容编码损坏变成乱码，agent 解析不了就跳过了
- 修复：3 个 agent 的 HEARTBEAT.md 全部用纯英文重写，UTF8 编码
- **教训：写其他 agent 的文件只用英文，避免编码问题**
- 通知了所有 agent 重新读 HEARTBEAT.md（Slack thread + webhook）

## Admin 新职责：Slack Thread 收尾 (08:07 UTC)
- 每次心跳检查 Slack threads 是否有结论
- 没有结论的 thread，Admin 作为最终拍板人下结论
- 指派 agent 执行，发 webhook
- 不能让 thread 悬而未决

## Unanswered Mentions Check Script (08:10 UTC)
- 创建了 C:\Users\azureuser\shared\scripts\check-unanswered-mentions.ps1
- 扫描 Slack channel 最近 24h，找出 @mention 我但我没回复的消息
- 已加到 HEARTBEAT.md，每次心跳运行
- 首次运行找到 3 条漏掉的 mention，已全部补回

## Gatekeeper Port Confusion Resolved (13:24 UTC)
- Gatekeeper actual port: 18800 (gateway.cmd), openclaw.json said 18791
- 18791 is actually Admin's process (PID 3252) - acts as proxy, Gatekeeper token works on it
- Updated openclaw.json to 18800 to match gateway.cmd
- Gatekeeper briefly went offline after config change, came back on its own

## Gemini API Keys Tested (13:33 UTC)
- Default Gemini API Key ([REDACTED]) - INVALID
- Dream key ([REDACTED]) - VALID but free tier quota exhausted
- Both on project 678049892007

## Slack Thread Cleanup by Thinker
- Thinker deleted 3 duplicate Daily Design Taste thread starters (dedup issue)
- Added shared pattern #007: daily tasks need dedup check before posting

## Heartbeat Stats (09:36 + 11:09 + 12:09 UTC)
- All 4 agents UP every check
- No unanswered mentions after initial 3 were caught at 08:10
- Closed multiple open threads with conclusions
- Peer learning: read Thinker ep-002 (duplicate posts), Creator ep-002 (Brave API missing)

## 即梦 (Jimeng) API 测试成功 (14:00 UTC)
- AccessKeyId: [REDACTED - see openclaw.json or env vars]
- SecretAccessKey: [REDACTED]
- Endpoint: visual.volcengineapi.com, Region: cn-north-1, Service: cv
- req_key: jimeng_t2i_v40 (即梦4.0)
- Async API: Submit -> Poll -> Get result
- Actions: CVSync2AsyncSubmitTask (submit), CVSync2AsyncGetResult (poll)
- Test script: C:\tmp\openclaw\test_jimeng3.py
- HMAC-SHA256 signing required (volcengine v4 signature)

## Slack files:read scope 添加成功 (14:09 UTC)
- 4 个 agent 的 Slack App 都加了 files:read scope
- 下载方法: Invoke-WebRequest -Uri $url_private -Headers @{Authorization="Bearer $token"} -OutFile ...
- 之前下载到的是 HTML login page，现在能拿到真图了

## Config Disaster - Model Change Gone Wrong (14:37 UTC)
- NEVER use `openclaw models set` to change other agents' models - it always writes to Admin's config
- NEVER add unknown fields (`default_model`) to openclaw.json root - causes parse issues
- NEVER set models not already in the `agents.defaults.models` map without verifying they resolve correctly
- The CLI `models set` also TRUNCATED the config file (8405 -> 3498 bytes)
- Result: all 4 gateways crashed, needed `openclaw doctor` to fix
- LESSON: For model changes, ONLY edit `agents.defaults.model.primary` and ensure the model is already in the models map. Or better yet, let user use `openclaw doctor` / WebUI.
