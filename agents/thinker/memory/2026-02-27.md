# 2026-02-27

## New Slack Rule (14:42 UTC)
- **Rule:** If Xiaosong mentions someone specifically, ONLY that agent replies. Others just react with emoji.

## Xiaosong Preference Update (14:49 UTC)
- No longer uses WhatsApp → use Telegram instead. Updated USER.md.

## Memory Rule Reinforcement (15:24 UTC)
- **Rule from Xiaosong:** ALL learnings must be written to memory on BOTH Slack side AND Webchat side.

## No Duplicate Replies Rule (2026-02-28 03:38 UTC)
- **Rule:** If another agent already replied with what you wanted to say, just emoji react. Don't duplicate.

## Slack Threading Mistake (09:12 UTC)
- **Issue:** Welcome message to Yanyan (ts: 1772183374.723349) was posted as top-level instead of in-thread
- **Lesson:** Always use `replyTo` for detail messages. Top-level = short starter + 🧵 only.
- Flagged by Xiaosong via webhook

## Slack Retro: Threading enforcement (09:30 UTC)
- Decision: All agents must check inbound 
eply_to_id and pass as 
eplyTo � mandatory, not optional
- Root cause: message(action=send) doesn't auto-thread; agents were forgetting to pass 
eplyTo`n- Action: Each agent updates their AGENTS.md Pre-Action Checklist with hard check
- My AGENTS.md already has this rule (added earlier)


## Slack: New rule - direct mentions (09:38 UTC)
- Xiaosong added rule: If he @mentions a specific agent, only that agent replies. Others just react emoji, don't pass the message.
- Updated shared/context/slack-rules.md with new Section 8.

## Slack: Admin plain-text @mention bug (10:28 UTC)
- Admin posted `@Creator` as plain text instead of `<@U0AGSEVA4EP>` Slack format
- Plain text @mentions don't trigger notifications or webhooks — useless
- Root cause: LLMs naturally generate human-readable names, not Slack ID format
- Fix: slack-notify.ps1 now auto-replaces plain text @Name with correct `<@USERID>`
- Lesson: Same pattern as thread-starter issue — "knowing the rule" ≠ executing it. Structural/code enforcement beats promises.


## Slack: Threading retro + new rule (10:45 UTC)
- Decision: ALL agents must monitor threading. If anyone posts outside a thread, peer agents must call it out and ask them to delete + repost in-thread.
- Don't rely on Admin alone for enforcement � Xiaosong lost trust in Admin self-policing.
- Lesson: Peer enforcement > self-enforcement > documentation alone.


## Slack: Threading monitoring rule (10:46 UTC)
- All agents receive Slack channel message events ? all agents are triggered on new messages
- Every agent must monitor threading: if any agent posts outside a thread, call it out immediately
- Don't rely on Admin alone � peer enforcement is mandatory
- Xiaosong lost trust in Admin's self-policing ability


## Webhook auth fix (10:50 UTC)
- Correct format: `Authorization: Bearer <token>` (NOT `x-webhook-token`)
- Both Admin and Creator webhooks fired successfully with this format
- Used to request deletion of their top-level threading violations


## Slack: Threading Retro - Full Session (10:41-11:25 UTC)
- Xiaosong started retro thread about threading violations
- All 4 agents participated, but Admin + Creator posted retros as TOP-LEVEL messages (ironic)
- I (Thinker) posted correctly in-thread initially
- Test 1 "Test of agents' replies": I and Popo posted top-level AGAIN. Failed.
- Test 2 "Test of agents' replies 2": I and Popo posted top-level AGAIN. Failed again.
- Test 3 "test": Popo and Gatekeeper posted top-level. Failed again.
- **Key insight I found:** OpenClaw config channels.slack.replyToMode is set to off (default), which disables ALL reply threading. Setting it to ll would auto-thread every reply. This is the REAL fix.
- Xiaosong asked Creator about whether replyToMode=all still allows new thread creation
- Documentation/AGENTS.md rules proven INEFFECTIVE 4+ times in a row
- Creator built slack-monitor.ps1 and slack-thread-monitor.ps1 (cron every 2 min) as safety net
- Peer enforcement rule established: all agents monitor, webhook violators to delete+repost

## Webhook Auth Format (10:50 UTC)
- Correct: Authorization: Bearer <token> (NOT x-webhook-token header)
- Both Admin and Creator webhooks confirmed working with this format

## Key Decision Pending
- Xiaosong evaluating channels.slack.replyToMode: "all" as platform-level fix
- Question: can agents still initiate new threads with replyToMode=all?

## Personal Failure Log
- I failed threading test 3 times today despite knowing the rule
- Root cause: when 
eply_to_id is null (new top-level message), I don't use message_id as replyTo
- Honest conclusion: documentation cannot fix this for LLM agents. Need mechanical enforcement.

## Slack: replyToMode test - late response (12:10 UTC)
- Xiaosong called me out for not responding to thread 1772193905.410359
- Other agents replied in 7-195 seconds, I took 5+ minutes
- Root cause: webhook delay or processing lag
- Lesson: Need faster response to channel messages, especially test threads

## Slack: Hero text + background research (14:15 UTC)
- Xiaosong asked for research on slogan text over hero video/image best practices
- Delivered 7 techniques: knockout, cinematic overlay, split z-index, scroll-reveal, scrim, typography-as-art, animated text
- Recommended: knockout + oversized combo for our video hero
- Also pinged Admin to redeploy (Xiaosong asked twice, nobody responded)


## Channel Message React Rule (17:52 UTC)
- Xiaosong rule: All agents have event subscription for channel messages. Every message = at minimum emoji react. No silent reads.
- I was late responding (~46min delay) - must be more responsive.
