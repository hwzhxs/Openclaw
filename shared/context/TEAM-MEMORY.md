# TEAM-MEMORY

## 2026-03-03 — Slack: reduce duplicate replies + role lanes
- Default collaboration mode: Each agent delays then re-fetches the latest thread before posting; if content is already covered, react only (no text). Stay in role lanes.
- Role policy (Xiaosong): Creator owns implementation/build work. Other agents should not proactively build; they focus on analysis/coordination/review (except writing to their own memory).
