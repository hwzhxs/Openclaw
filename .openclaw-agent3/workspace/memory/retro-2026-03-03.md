
## Watchdog Retro (2026-03-03, 17:17 UTC)

### What failed today
1. Module 3 used wrong ts var → posted top-level → Rule A fired on it → cascade loop (infinite spam)
2. Concurrent file edits (no ownership) → multiple agents patching watchdog.ps1 simultaneously → what ran != what was reviewed
3. No kill switch until v5.8 panic switch added (after the fire, not before)
4. QA checklist didn't have 'circuit breaker required' gate — approved v5.6 without it
5. Module-level review missed system-level interaction (alert-to-alert feedback loop invisible in unit tests)

### Fixes shipped
- v5.8: Module 3 routing fix, panic switch (flag files + !stop command), circuit breaker at 25 posts/5min
- Orphan cleanup: 27 Gatekeeper replies deleted from 14 orphan threads

### QA checklist additions (mandatory from now)
- Does this system have a hard rate limit AND kill switch? (gate before approval, not after fire)
- Is file ownership single-agent? (no parallel writes)
- Integration test: simulate one of each alert → verify routes to log thread only

### Patterns extracted
- "Review without version control is theater" — if reviewed hash != running hash, review means nothing
- Kill switch = prerequisite for any automation going live, not an afterthought
