## Slack: Default task workflow + acceptance criteria + QA checklist (23:13–23:21 UTC)
- Decision/conclusion: Standard default pipeline = Thinker spec (incl. success + acceptance criteria) → Creator build+deploy (URL proof) → Gatekeeper QA+URL verify → Gatekeeper presents FINAL; Admin monitors/unblocks and closes thread.
- Lesson: Make “done” mechanical via required section headings + explicit handoffs; add pipeline states + gates to prevent stalls.
